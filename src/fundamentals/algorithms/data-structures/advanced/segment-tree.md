# Segment Tree (Дерево отрезков)

Запросы и обновления на диапазонах за O(log n); каждый узел хранит агрегат своего сегмента — сумму, минимум, максимум.

## Возможности

- Lazy propagation для массовых обновлений отрезков.
- Универсальнее дерева Фенвика для range queries (см. fenwick-tree).
