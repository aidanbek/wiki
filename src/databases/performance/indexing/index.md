Структуры данных для ускорения поиска без full table scan. Trade-off: faster reads ↔ slower writes + storage overhead.
Index selection критичен: неправильные индексы хуже чем их отсутствие (maintenance cost без query benefit).
