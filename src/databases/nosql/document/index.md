Хранение semi-structured данных (JSON/BSON documents). Schema-less (гибкость) или schema validation. Embedding vs
referencing для relationships. Queries на nested fields. Horizontal scaling через sharding.
