Distributed column-family DB без single point of failure. Tunable consistency (quorum reads/writes). Ring architecture (
consistent hashing). High write throughput, linear scalability. CQL (похож на SQL). Use cases: time-series, IoT,
messaging.
