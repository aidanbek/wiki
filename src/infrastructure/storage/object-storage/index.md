Flat namespace, objects addressed по unique key. Metadata-rich, HTTP access (REST APIs). Highly scalable (petabytes),
eventual consistency обычно. S3-compatible APIs стандарт. Use cases: static assets, backups, data lakes. No file system
overhead, cheaper чем block storage at scale.
